<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.motion.widget.MotionLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:ignore="MotionLayoutInvalidSceneFileReference">

    <!-- Logo o imagen del banco -->

    <!-- Texto del nombre del banco -->

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:src="@drawable/banco_pichincha_logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.186"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/banco_pichincha"
        android:textAllCaps="true"
        android:textColor="#00003C"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/logoImageView"
        app:layout_constraintTop_toTopOf="@+id/logoImageView" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Versión: 9.5.0"
        android:textColor="#000000"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/lottieAnimationView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="30dp"
        app:layout_constraintStart_toEndOf="@+id/logoImageView"
        app:layout_constraintTop_toTopOf="@+id/logoImageView"/>

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottieAnimationView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        app:lottie_rawRes="@raw/animacion"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintTop_toBottomOf="@+id/logoImageView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.33" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cuida tus contraseñas, no las compartas con nadie."
        android:textColor="#03A9F4"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/lottieAnimationView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <!-- Mueve el LinearLayout más abajo -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
    app:layout_constraintTop_toBottomOf="@+id/lottieAnimationView"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintHorizontal_bias="0.5">

    <!-- Botón para usuario y contraseña -->
    <ImageButton
        android:id="@+id/btnUsuarioContrasena"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_usuario_contrasena"
        android:background="@android:color/transparent"
        />

    <!-- Espaciador para crear un espacio entre los botones -->
    <Space
        android:layout_width="16dp"
        android:layout_height="48dp" />

    <!-- Botón para logear con Huella/face ID -->
    <ImageButton
        android:id="@+id/btnHuellaFaceID"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_huella_face_id" />

    <!-- Espaciador para crear un espacio entre los botones -->
    <Space
        android:layout_width="16dp"
        android:layout_height="48dp" />

    <!-- Botón para logear con Pin de 6 dígitos -->
    <ImageButton
        android:id="@+id/btnPin6Digitos"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_pin_6_digitosss" />

</LinearLayout>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        app:layout_constraintTop_toBottomOf="@+id/lottieAnimationView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5">

        <!-- Botón para usuario y contraseña -->
        <ImageButton
            android:id="@+id/btnUsuarioContrasena"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ubicacion"
            android:background="@android:color/transparent"
            />

        <!-- Espaciador para crear un espacio entre los botones -->
        <Space
            android:layout_width="16dp"
            android:layout_height="48dp" />

        <!-- Botón para logear con Huella/face ID -->
        <ImageButton
            android:id="@+id/btnHuellaFaceID"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/clave_digital" />

        <!-- Espaciador para crear un espacio entre los botones -->
        <Space
            android:layout_width="16dp"
            android:layout_height="48dp" />

        <!-- Botón para logear con Pin de 6 dígitos -->
        <ImageButton
            android:id="@+id/btnPin6Digitos"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/llamanos" />


    </LinearLayout>
<LinearLayout
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="230dp"
    app:layout_constraintTop_toBottomOf="@+id/lottieAnimationView"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintHorizontal_bias="0.5">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="¿Ingresa con otro usuario?"
        android:textColor="#03A9F4"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/lottieAnimationView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />


</LinearLayout>

    </androidx.constraintlayout.motion.widget.MotionLayout>
